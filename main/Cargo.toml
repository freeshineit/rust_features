[package]
name = "main"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
feat_demo = { path = '../feat_demo', features = ["webp"] }


[features]
webp = ['feat_demo/webp']